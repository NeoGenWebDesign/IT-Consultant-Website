# IT Instructor — Homepage

Simple static homepage and contact dashboard.

Files:

- `index.html` — Homepage with hero, about section, courses & services listing.
- `dashboard.html` — Contact dashboard with WhatsApp and email links.
- `styles.css` — Styles for both pages.

To preview locally:

```bash
# from the project folder
python3 -m http.server 8000
# then open http://localhost:8000 in your browser
```

Contact details (as provided):

- WhatsApp: `+27 82 82 83 843` (link opens via `https://wa.me/27828283843`)
- E-mail: `itdidactix@gmail.com`

If you want, I can:

- Add a contact form that posts to an external endpoint or sends a message via serverless function.
- Generate a simple Node/Python backend to accept messages.
